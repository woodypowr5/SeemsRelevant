var app = angular.module('sortingApp', []);

 // app.run(function($rootScope) {
 //    // Receive emitted messages and broadcast them to child scopes.
    
 //    // $rootScope.$on('emitSetActiveMatch', function(event, args) {
 //    //     $rootScope.$broadcast('broadcastSetActiveMatch', args);
 //    // });
 // }   
